"""
    .gitignore사용법
    1. .gitignore 파일을 만든다.
    2. .gitignore 파일에 무시할 파일이나 폴더를 적는다.
    -> 파일인 경우 파일명만 적으면 된다.
    -> 폴더인 경우 폴더명만 적으면 된다.
    -> 주석은 #으로 시작한다.
    3. .gitignore 파일을 저장한다.

"""


kiwoom32/
Business/

.gitignore